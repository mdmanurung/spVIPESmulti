"""Regression tests for findings in audits/2026-05-08-full-package*.md.

These tests are skeletons. Each is marked ``xfail(strict=True)`` until the
matching work item in ``ImplementationPlan_AuditRemediation.md`` is
implemented; at that point the ``xfail`` marker is removed and the test
becomes a hard regression guard.

See ``ImplementationPlan_AuditRemediation.md`` for the work-item map.
"""
